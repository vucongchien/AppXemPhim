<?xml version="1.0" encoding="utf-8"?>
    <shape xmlns:android="http://schemas.android.com/apk/res/android">
        <solid android:color="@color/bg_input_container" /> <!-- Màu nền từ colors.xml -->
        <stroke
            android:width="2dp"
            android:color="@color/text_login" /> <!-- Viền màu nâu đỏ -->
        <corners android:radius="8dp" /> <!-- Bo góc -->
        <padding
            android:left="8dp"
            android:top="8dp"
            android:right="8dp"
            android:bottom="8dp" />
    </shape>
